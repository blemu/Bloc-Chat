(function() {
     function config($stateProvider) {
         $stateProvider
         .state();   
     }
    angular
         .module('blocChat', ['ui.router', 'firebase'])
         .config(config);
 })();